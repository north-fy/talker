apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f ./server-gateway/docker-compose.yml -o ./deployment/server
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: server-gateway
  name: server-gateway
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
  selector:
    io.kompose.service: server-gateway
