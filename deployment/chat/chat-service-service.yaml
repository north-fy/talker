apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f ./services/chat/docker-compose.yml -o ./deployment/chat
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: chat-service
  name: chat-service
spec:
  ports:
    - name: "50053"
      port: 50053
      targetPort: 50053
  selector:
    io.kompose.service: chat-service
